name: tests
on: [push]
jobs:
  minimax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./test/winloss minimaxSearch
        shell: bash
  alphabeta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./test/winloss alphaBetaSearch
        shell: bash
