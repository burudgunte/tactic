class Square {
    constructor(localGame, state = 0) {
        this._localGame = localGame;
        this._state = state;
    }

    get state() {
        return this._state;
    }

    makeMove(player) {
        state = player;
    }
}

export { Square };